/**
 * @deprecated Moved to `@/server/auth`.
 *
 * Kept as a re-export so the 11 existing importers keep compiling unchanged.
 */
export { isAdmin, isCoordinator, isPaymentAdmin } from '@/server/auth/auth-helpers';

