/**
 * Typed accessors for the design tokens declared in src/styles/tokens.css.
 *
 * The CSS file is the single source of truth for values; anything exported here
 * exists so TypeScript callers (chart configs, inline SVG, canvas) can reference
 * a token without duplicating its literal value.
 */
export {};
